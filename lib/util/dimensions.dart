class Dimensions {
  // Padding
  static const mediumPadding = 16.0;
  static const smallPadding = 8.0;

  // Elevation
  static const mediumElevation = 10.0;

  // Color shade
  static const colorShade = 50;

  // Font size
  static const mediumFontSize = 16.0;
  static const largeFontSize = 20.0;

  // Icon
  static const iconSize = 50.0;
  static const iconHeight = 100.0;
  static const iconWidth = 70.0;

  // Image
  static const imageSize = 250.0;
  static const detailScreenBackgroundColorShade = 50;
  static const imageHeight = 200.0;
  static const imageWidth = 250.0;

  // SizedBox
  static const sizeBoxHeight = 20.0;

  // Border radius
  static const borderRadius = 16.0;
}
